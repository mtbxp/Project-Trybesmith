export type TProduct = {
  id?: number,
  name: string,
  amount: string,
  orderId?: number,
};

export type TUser = {
  id: number,
  userName: string,
  vocation: string,
  password: string,
};

export type Toder = {
  id: number,
  userId: number,
};
