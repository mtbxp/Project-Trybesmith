export type TProducts = {
  id?: number,
  name: string,
  amount: string,
  orderId?: number | null | undefined,
};

export type TUsers = {
  username: string,
  vocation: string,
  level: number,
  password: string,
};